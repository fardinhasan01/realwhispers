# Cloudinary — public client config only (no API secret in frontend)
# Create unsigned preset: Cloudinary Console → Settings → Upload → Upload presets
# Signing mode: Unsigned, name must match VITE_CLOUDINARY_UPLOAD_PRESET
VITE_CLOUDINARY_CLOUD_NAME=ddyqow8bi
VITE_CLOUDINARY_UPLOAD_PRESET=whisperlock_unsigned
